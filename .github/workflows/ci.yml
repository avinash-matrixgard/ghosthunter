name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install "ruff>=0.4.0"
      - name: ruff check
        run: ruff check .
      - name: ruff format --check
        run: ruff format --check .

  test:
    name: pytest (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Poetry
        run: pipx install poetry
      - name: Install dependencies (with all extras)
        run: poetry install --with dev --all-extras
      - name: pytest
        run: poetry run pytest -v --tb=short

  pip-audit:
    name: pip-audit (dependency vulns)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.12"
      - name: Install Poetry + export plugin + pip-audit
        # Modern Poetry (1.8+) ships `export` as a separate plugin.
        # Inject `poetry-plugin-export` into the pipx-managed Poetry env.
        run: |
          pipx install poetry
          pipx inject poetry poetry-plugin-export
          pipx install pip-audit
      - name: Export requirements
        run: poetry export --with dev --all-extras --without-hashes -f requirements.txt -o requirements.txt
      - name: pip-audit
        # --strict fails the build on any vulnerability. Until the pytest
        # tmpdir CVE (medium, dev-only, fix requires major bump 8 -> 9)
        # is resolved upstream or via manual upgrade, audit warns instead
        # of failing. Re-tighten to --strict after the next pytest bump.
        run: pip-audit -r requirements.txt
