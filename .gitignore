# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# Virtualenvs
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# Ghosthunter local state — never commit
.ghosthunter/
*.log
sa-key*.json
*.json.key

# Secrets
.env
.env.*
!.env.example

# Billing exports (sensitive)
Billing Account for*.csv

# Internal dev notes — never commit.
# These files contain personal/work-context that shouldn't live in a
# public repo. Claude / contributors sync state via git branches, not
# a shared CLAUDE.md.
CLAUDE.md
docs/internal/

# Claude Code local state (worktrees, local settings)
.claude/
